# Synthetic identity fixture — staleness guard regression

Not a real profile. No names, no paths, no preferences. Its only job is to
mention one flag that `/proxyme` no longer documents and one that it does.

## 7. Proxy operational rules

Deactivate the proxy for this session with `--off`.
Suppress new spawns for this session with `--nonew`.
